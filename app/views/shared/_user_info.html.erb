<%= link_to gravatar_for(current_user, 80), current_user %>
<div class = "text">
  <h3><%= current_user.name %></h3>
  <span><%= link_to "view my profile", current_user %></span>
  <span><%= pluralize(current_user.microposts.count, "micropost") %></span>
</div>
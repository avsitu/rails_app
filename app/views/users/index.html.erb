<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <div>
      <%= gravatar_for user, 50 %>
      <%= link_to "#{user.name} - #{user.username}", user %>
      </div>
    </li>
  <% end %>
</ul>